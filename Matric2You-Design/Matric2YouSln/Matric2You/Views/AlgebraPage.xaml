<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
 x:Class="Matric2You.Views.AlgebraPage"
 Title="Algebra">
 <Grid RowDefinitions="Auto,Auto,*" Padding="16" RowSpacing="12">
 <Label Grid.Row="0" Text="Algebra Study Guide" FontAttributes="Bold" FontSize="22" />
 <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="8">
 <Label Text="Progress:" VerticalOptions="Center"/>
 <ProgressBar x:Name="Progress" Grid.Column="1" Progress="0" WidthRequest="180" />
 </Grid>
 <ScrollView x:Name="Scroller" Grid.Row="2">
 <VerticalStackLayout Spacing="12">
 <Border x:Name="Sec1" Padding="12" StrokeThickness="0" Background="{AppThemeBinding Light=#F7F7F7, Dark=#222}">
 <VerticalStackLayout Spacing="6">
 <Label Text="Section1: Basics" FontAttributes="Bold" />
 <Label Text="- Variable: a symbol (like x) representing a number." />
 <Label Text="- Expression: combination of numbers/variables/operations (e.g.,2x +3)." />
 <Label Text="- Equation: states two expressions are equal (e.g.,2x +3 =9)." />
 </VerticalStackLayout>
 </Border>

 <Border x:Name="Sec2" Padding="12" StrokeThickness="0" Background="{AppThemeBinding Light=#F7F7F7, Dark=#222}">
 <VerticalStackLayout Spacing="6">
 <Label Text="Section2: Solving Linear Equations" FontAttributes="Bold" />
 <Label Text="Example:2x +6 =14 ->2x =8 -> x =4" />
 <Label Text="Steps: combine like terms, isolate x, keep the equation balanced." />
 </VerticalStackLayout>
 </Border>

 <Border x:Name="Sec3" Padding="12" StrokeThickness="0" Background="{AppThemeBinding Light=#F7F7F7, Dark=#222}">
 <VerticalStackLayout Spacing="6">
 <Label Text="Section3: Factoring" FontAttributes="Bold" />
 <Label Text="Difference of squares: a^2 - b^2 = (a - b)(a + b)." />
 <Label Text="Example: x^2 -9 = (x -3)(x +3)." />
 </VerticalStackLayout>
 </Border>

 <Border x:Name="Sec4" Padding="12" StrokeThickness="0" Background="{AppThemeBinding Light=#F7F7F7, Dark=#222}">
 <VerticalStackLayout Spacing="6">
 <Label Text="Section4: Linear Functions" FontAttributes="Bold" />
 <Label Text="Slope-intercept form: y = m*x + b (m: slope, b: y-intercept)." />
 </VerticalStackLayout>
 </Border>

 <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
 <Label x:Name="SectionLabel" Text="Section1 of4" VerticalOptions="Center" />
 <HorizontalStackLayout Grid.Column="1" Spacing="8">
 <Button Text="Prev" Clicked="OnPrevClicked" />
 <Button Text="Next" Clicked="OnNextClicked" />
 </HorizontalStackLayout>
 </Grid>

 <Button Text="Back" Clicked="OnBackClicked" />
 </VerticalStackLayout>
 </ScrollView>
 </Grid>
</ContentPage>
